const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
  card:{
    width:'100%',
    backgroundColor:'#fff',
    borderRadius:15,
    elevation:4,
    padding:8,
    height:65,
    alignSelf:'center'

  }

})

export default styles